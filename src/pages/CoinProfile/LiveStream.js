import React from 'react';
import { Text } from 'react-native';
const liveStream = (props) => {
  return (
    <Text>Still in the work, but watch this space</Text>
  );
};

export default liveStream;
