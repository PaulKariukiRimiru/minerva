export const users = {}; // Initial state for user reducer
export const coins = { coinList: [], coinPrices: [] }; // Initial state for coin reducer
export const coinProfile = {
  profile: {},
  aggregate: {},
  history: {},
}; // Initial state for the coin profile reducer
export const activeCallState = 0; // Initial state for activeCalls reducer
export const coinSocial = {}; // Initial state for the coin social reducer
